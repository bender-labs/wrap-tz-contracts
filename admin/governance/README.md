the oracle role of the governance token uses a specialized multisig, because generating a 
long list of transfer in a lambda is not very practical. 

use the governance helper script to manipulate this part.